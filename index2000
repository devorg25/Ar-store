<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="description" content="Ar store — Vente de vêtements en gros et détails">
<title>Ar store — Arstore AR</title>
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<style>
:root{
  --bg:#080a0d;--card:rgba(18,22,28,.82);--text:#f5f5f5;--muted:#a8adb7;
  --line:rgba(255,255,255,.10);--accent:#fff;--danger:#ff6678;--ok:#71e6ae;
}
*{box-sizing:border-box;margin:0;padding:0}
html{scroll-behavior:smooth}
body{font-family:Inter,Arial,sans-serif;background:var(--bg);color:var(--text);min-height:100vh;overflow-x:hidden}
button,input,select,textarea{font:inherit}
button{cursor:pointer;border:0}
a{color:inherit;text-decoration:none}

/* Dynamic background */
#colorBg{position:fixed;inset:0;z-index:-3;background:#080a0d;transition:background 1s ease}
#colorBg:after{content:"";position:absolute;inset:-20%;background:radial-gradient(circle at 20% 20%,rgba(255,255,255,.07),transparent 25%),radial-gradient(circle at 80% 70%,rgba(255,255,255,.05),transparent 30%);animation:floatBg 10s ease-in-out infinite alternate}
@keyframes floatBg{to{transform:translate(4%, -3%) scale(1.08)}}

/* Header */
header{position:sticky;top:0;z-index:20;background:rgba(7,9,12,.76);backdrop-filter:blur(18px);border-bottom:1px solid var(--line)}
.nav{max-width:1200px;margin:auto;height:76px;display:flex;align-items:center;justify-content:space-between;padding:0 18px}
.brand{display:flex;align-items:center;gap:12px;font-weight:700;letter-spacing:.8px}
.logo{width:46px;height:46px;border:1px solid rgba(255,255,255,.55);border-radius:50%;display:grid;place-items:center;position:relative;background:#050608;overflow:hidden}
.logo span{font-size:21px;font-weight:300;letter-spacing:-5px;transform:translateX(-2px)}
.logo:before{content:"";position:absolute;width:32px;height:32px;border:1px solid rgba(255,255,255,.4);border-radius:50%}
.brand small{display:block;color:var(--muted);font-weight:400;font-size:10px;letter-spacing:2px;margin-top:2px}
.actions{display:flex;align-items:center;gap:9px}
.icon-btn{width:45px;height:45px;border-radius:50%;background:rgba(255,255,255,.07);border:1px solid var(--line);color:#fff;display:grid;place-items:center;font-size:21px;position:relative;transition:.25s}
.icon-btn:hover{transform:translateY(-2px);background:rgba(255,255,255,.13)}
.cart-count{position:absolute;right:-2px;top:-3px;background:#fff;color:#000;font-size:11px;min-width:19px;height:19px;border-radius:20px;display:grid;place-items:center;font-weight:700}

/* animated icons */
.spark{animation:spark 2.2s infinite}
@keyframes spark{0%,100%{transform:rotate(0) scale(1)}50%{transform:rotate(12deg) scale(1.12)}}
.pulse{animation:pulse 1.8s infinite}
@keyframes pulse{50%{box-shadow:0 0 0 8px rgba(255,255,255,.02)}}

main{max-width:1200px;margin:auto;padding:22px 18px 70px}
.hero{padding:55px 0 30px;display:grid;grid-template-columns:1.2fr .8fr;gap:30px;align-items:center}
.eyebrow{color:var(--muted);letter-spacing:4px;text-transform:uppercase;font-size:11px;margin-bottom:12px}
.hero h1{font-size:clamp(44px,8vw,88px);line-height:.9;letter-spacing:-4px;font-weight:700}
.hero h1 em{font-style:normal;font-weight:200}
.hero p{max-width:570px;color:#c4c8d0;margin-top:22px;line-height:1.7}
.hero-card{min-height:310px;border:1px solid var(--line);border-radius:30px;background:linear-gradient(145deg,rgba(255,255,255,.08),rgba(255,255,255,.015));display:grid;place-items:center;position:relative;overflow:hidden}
.hero-card .big-logo{font-size:130px;font-weight:200;letter-spacing:-24px;transform:translateX(-10px);opacity:.9}
.orbit{position:absolute;width:230px;height:230px;border:1px solid rgba(255,255,255,.15);border-radius:50%;animation:orbit 9s linear infinite}
@keyframes orbit{to{transform:rotate(360deg)}}
.orbit:after{content:"";width:7px;height:7px;background:#fff;border-radius:50%;position:absolute;top:-3px;left:50%}

.toolbar{display:flex;justify-content:space-between;align-items:center;gap:15px;margin:15px 0 24px}
.section-title{font-size:24px}
.filters{display:flex;gap:8px;flex-wrap:wrap}
.filter{padding:10px 15px;border-radius:999px;background:rgba(255,255,255,.06);color:#d7d9de;border:1px solid var(--line)}
.filter.active,.filter:hover{background:#fff;color:#000}

.products{display:grid;grid-template-columns:repeat(4,1fr);gap:15px}
.product{border:1px solid var(--line);border-radius:22px;overflow:hidden;background:var(--card);position:relative;transition:.3s}
.product:hover{transform:translateY(-5px);border-color:rgba(255,255,255,.22)}
.product-img{height:280px;background:linear-gradient(135deg,#16191e,#090a0d);display:grid;place-items:center;overflow:hidden;position:relative}
.product-img img{width:100%;height:100%;object-fit:cover}
.product-placeholder{font-size:56px;opacity:.22}
.product-info{padding:15px}
.product-info h3{font-size:16px;margin-bottom:6px}
.product-info p{font-size:12px;color:var(--muted);min-height:34px;line-height:1.4}
.price{font-size:18px;font-weight:700;margin:12px 0}
.add{width:100%;padding:12px;border-radius:13px;background:#fff;color:#000;font-weight:700}
.badge{position:absolute;top:12px;left:12px;padding:6px 9px;border-radius:999px;background:rgba(0,0,0,.62);backdrop-filter:blur(8px);font-size:10px;z-index:2}
.empty{grid-column:1/-1;padding:50px;text-align:center;color:var(--muted);border:1px dashed var(--line);border-radius:20px}

.info{margin-top:70px;border-top:1px solid var(--line);padding-top:35px}
.info-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px;margin-top:20px}
.info-card{padding:22px;border:1px solid var(--line);border-radius:20px;background:rgba(255,255,255,.035)}
.info-card .i{font-size:24px;margin-bottom:13px}
.info-card h3{font-size:14px;margin-bottom:7px}
.info-card p{color:#c4c8d0;line-height:1.6;font-size:14px}
.info-card a{text-decoration:underline;text-underline-offset:4px}

footer{max-width:1200px;margin:auto;padding:25px 18px 45px;color:var(--muted);display:flex;justify-content:space-between;gap:15px;border-top:1px solid var(--line);font-size:12px}
.admin-link{background:none;color:#b7bbc4;text-decoration:underline;text-underline-offset:4px;font-size:12px;font-weight:300}

/* Menu */
.overlay{position:fixed;inset:0;background:rgba(0,0,0,.6);backdrop-filter:blur(5px);z-index:40;display:none}
.overlay.show{display:block}
.side-menu{position:fixed;right:0;top:0;height:100%;width:min(370px,90vw);background:#0d1014;border-left:1px solid var(--line);z-index:41;padding:25px;transform:translateX(100%);transition:.35s;overflow:auto}
.side-menu.show{transform:translateX(0)}
.menu-top{display:flex;align-items:center;justify-content:space-between;margin-bottom:30px}
.menu-top h2{font-size:22px;font-weight:500}
.close{background:rgba(255,255,255,.08);color:#fff;width:40px;height:40px;border-radius:50%}
.menu-item{width:100%;display:flex;align-items:center;justify-content:space-between;padding:18px 0;border-bottom:1px solid var(--line);background:none;color:#fff;text-align:left;font-size:16px}
.menu-item span:first-child{display:flex;gap:12px;align-items:center}
.submenu{display:none;padding:0 0 5px 35px}
.submenu.show{display:block}
.submenu button{display:block;background:none;color:var(--muted);padding:10px 0;width:100%;text-align:left}

/* Cart drawer */
.drawer{position:fixed;right:0;top:0;height:100%;width:min(450px,94vw);background:#0c0f13;z-index:51;border-left:1px solid var(--line);transform:translateX(100%);transition:.35s;padding:22px;overflow:auto}
.drawer.show{transform:translateX(0)}
.cart-row{display:grid;grid-template-columns:58px 1fr auto;gap:12px;align-items:center;padding:14px 0;border-bottom:1px solid var(--line)}
.cart-thumb{width:58px;height:58px;border-radius:12px;background:#171a20;overflow:hidden;display:grid;place-items:center}
.cart-thumb img{width:100%;height:100%;object-fit:cover}
.qty{display:flex;gap:8px;align-items:center;margin-top:7px}
.qty button{width:24px;height:24px;border-radius:50%;background:#242831;color:#fff}
.remove{color:#ff7a89;background:none;font-size:11px}
.total{display:flex;justify-content:space-between;font-size:20px;font-weight:700;padding:20px 0}
.form{display:grid;gap:10px}
.form input,.form textarea,.form select{width:100%;background:#171b21;border:1px solid var(--line);color:#fff;padding:13px;border-radius:12px;outline:none}
.form textarea{min-height:80px;resize:vertical}
.primary{padding:13px;border-radius:12px;background:#fff;color:#000;font-weight:700}
.note{font-size:11px;color:var(--muted);line-height:1.5}

/* Modal/Admin */
.modal{position:fixed;inset:0;background:rgba(0,0,0,.72);backdrop-filter:blur(8px);z-index:60;display:none;place-items:center;padding:18px}
.modal.show{display:grid}
.modal-box{width:min(900px,100%);max-height:92vh;overflow:auto;background:#0d1014;border:1px solid var(--line);border-radius:24px;padding:22px}
.modal-head{display:flex;justify-content:space-between;align-items:center;margin-bottom:20px}
.admin-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:12px}
.panel{border:1px solid var(--line);border-radius:18px;padding:18px;background:rgba(255,255,255,.025)}
.panel h3{margin-bottom:14px;font-size:15px}
.admin-product{display:grid;grid-template-columns:55px 1fr auto;gap:10px;align-items:center;padding:10px 0;border-bottom:1px solid var(--line)}
.admin-product img{width:55px;height:55px;object-fit:cover;border-radius:10px;background:#171a20}
.small-btn{padding:8px 10px;border-radius:9px;background:#22262d;color:#fff;font-size:11px}
.danger{color:#ff8996}
.orders{display:grid;gap:10px}
.order{border:1px solid var(--line);border-radius:14px;padding:13px;background:#12161b}
.order-head{display:flex;justify-content:space-between;gap:10px}
.status{font-size:10px;padding:5px 8px;border-radius:99px;background:#242a31;color:#d9dce1}
.order ul{margin:9px 0 9px 18px;color:#c4c8d0;font-size:12px;line-height:1.5}
.login{max-width:380px;margin:auto}
.hidden{display:none!important}

@media(max-width:850px){
 .hero{grid-template-columns:1fr}.hero-card{min-height:220px}
 .products{grid-template-columns:repeat(2,1fr)}
 .info-grid{grid-template-columns:1fr}
}
@media(max-width:520px){
 .nav{height:66px}.brand .brand-text{display:none}
 main{padding:10px 12px 55px}.hero{padding:38px 0 20px}.hero h1{font-size:55px}
 .products{gap:10px}.product-img{height:220px}.product-info{padding:12px}
 .toolbar{align-items:flex-start;flex-direction:column}.filters{display:none}
 footer{flex-direction:column}
 .admin-grid{grid-template-columns:1fr}
}
</style>
</head>
<body>
<div id="colorBg"></div>

<header>
  <nav class="nav">
    <a class="brand" href="#">
      <div class="logo"><span>AR</span></div>
      <div class="brand-text">Ar store<small>ARSTORE AR</small></div>
    </a>
    <div class="actions">
      <button class="icon-btn pulse" onclick="openCart()" aria-label="Panier">🛒<b id="cartCount" class="cart-count">0</b></button>
      <button class="icon-btn spark" onclick="openMenu()" aria-label="Menu">⋮</button>
    </div>
  </nav>
</header>

<main>
  <section class="hero">
    <div>
      <div class="eyebrow">Clothing Brand</div>
      <h1>Ar <em>store</em></h1>
      <p>Vente de vêtements en gros et détails. Découvrez les pièces disponibles et choisissez votre catégorie depuis le menu.</p>
    </div>
    <div class="hero-card">
      <div class="orbit"></div>
      <div class="big-logo">AR</div>
    </div>
  </section>

  <section>
    <div class="toolbar">
      <h2 class="section-title" id="productsTitle">Toutes les pièces</h2>
      <div class="filters" id="filters"></div>
    </div>
    <div class="products" id="products"></div>
  </section>

  <section class="info" id="info">
    <div class="eyebrow">Arstore AR</div>
    <h2 class="section-title">Informations du magasin</h2>
    <div class="info-grid">
      <div class="info-card">
        <div class="i">☎</div><h3>Téléphone</h3>
        <p><a href="tel:0550686806">0550686806</a></p>
      </div>
      <div class="info-card">
        <div class="i">📍</div><h3>Magasin 1</h3>
        <p>BELLE VUE CONSTANTINE<br>11h-21h</p>
      </div>
      <div class="info-card">
        <div class="i">📍</div><h3>Magasin 2</h3>
        <p>NOUVELLE VILLE EN FACE UNIVERSITÉ 2<br>11h-21h</p>
      </div>
    </div>
  </section>
</main>

<footer>
  <div>© <span id="year"></span> Ar store — Arstore AR</div>
  <button class="admin-link" onclick="openAdmin()">Admin</button>
</footer>

<!-- Category menu -->
<div class="overlay" id="overlay" onclick="closeAll()"></div>
<aside class="side-menu" id="sideMenu">
  <div class="menu-top"><h2>Catégories</h2><button class="close" onclick="closeMenu()">×</button></div>
  <button class="menu-item" onclick="showCategory('all')"><span>✦ Toutes les pièces</span><span>›</span></button>
  <button class="menu-item" onclick="toggleSub('teeSub')"><span>👕 T-shirt</span><span>+</span></button>
  <div class="submenu" id="teeSub"><button onclick="showCategory('tshirt')">Tous les T-shirt</button></div>
  <button class="menu-item" onclick="toggleSub('pantSub')"><span>👖 Pant</span><span>+</span></button>
  <div class="submenu" id="pantSub">
    <button onclick="showCategory('pant-normal')">Pant — Normal</button>
    <button onclick="showCategory('pant-baggy')">Pant — Baggy</button>
  </div>
  <button class="menu-item" onclick="showCategory('shoes')"><span>👟 Shoes</span><span>›</span></button>
  <button class="menu-item" onclick="showCategory('accessories')"><span>🧢 Accessories</span><span>›</span></button>
  <div style="margin-top:30px;color:var(--muted);font-size:12px;line-height:1.7">
    <b style="color:#fff">Arstore AR</b><br>
    Vente de vêtements en gros et détails<br>
    Tel: 0550686806
  </div>
</aside>

<!-- Cart -->
<aside class="drawer" id="cartDrawer">
  <div class="modal-head"><h2>Votre panier</h2><button class="close" onclick="closeCart()">×</button></div>
  <div id="cartItems"></div>
  <div class="total"><span>Total</span><span id="cartTotal">0 DA</span></div>
  <form class="form" onsubmit="submitOrder(event)">
    <input id="customerName" required placeholder="Nom et prénom">
    <input id="customerPhone" required placeholder="Numéro de téléphone">
    <textarea id="customerAddress" required placeholder="Adresse de livraison"></textarea>
    <textarea id="customerNote" placeholder="Note (taille, couleur, etc.)"></textarea>
    <button class="primary" type="submit">Envoyer la demande de livraison</button>
    <div class="note">La commande est enregistrée dans ce prototype. Plus tard, remplacez la fonction d'enregistrement par votre table Supabase.</div>
  </form>
</aside>

<!-- Admin -->
<div class="modal" id="adminModal">
  <div class="modal-box">
    <div class="modal-head"><h2>Administration — Ar store</h2><button class="close" onclick="closeAdmin()">×</button></div>

    <div id="loginView" class="login">
      <p style="color:var(--muted);margin-bottom:15px">Accès réservé au propriétaire du magasin.</p>
      <form class="form" onsubmit="adminLogin(event)">
        <input id="adminUser" required placeholder="Compte">
        <input id="adminPass" type="password" required placeholder="Mot de passe">
        <button class="primary">Se connecter</button>
        <p class="note">Prototype uniquement : les identifiants sont contrôlés côté navigateur. Pour un vrai site, utilisez Supabase Auth + RLS.</p>
      </form>
    </div>

    <div id="adminView" class="hidden">
      <div class="admin-grid">
        <div class="panel">
          <h3>Ajouter une pièce</h3>
          <form class="form" onsubmit="addProduct(event)">
            <input id="pName" required placeholder="Nom de la pièce">
            <select id="pCat" required>
              <option value="tshirt">T-shirt</option>
              <option value="pant-normal">Pant — Normal</option>
              <option value="pant-baggy">Pant — Baggy</option>
              <option value="shoes">Shoes</option>
              <option value="accessories">Accessories</option>
            </select>
            <input id="pPrice" type="number" min="0" required placeholder="Prix en DA">
            <input id="pColor" required placeholder="Couleur (ex: noir)">
            <textarea id="pDesc" placeholder="Description"></textarea>
            <input id="pImage" type="file" accept="image/*">
            <button class="primary">Ajouter la pièce</button>
          </form>
        </div>

        <div class="panel">
          <h3>Pièces en vente</h3>
          <div id="adminProducts"></div>
        </div>
      </div>

      <div class="panel" style="margin-top:12px">
        <h3>Commandes des clients</h3>
        <div class="orders" id="adminOrders"></div>
      </div>

      <button class="small-btn" style="margin-top:15px" onclick="adminLogout()">Se déconnecter</button>
    </div>
  </div>
</div>

<script>
const SUPABASE_URL = 'https://zfoepbgvnwmxeukxqjdr.supabase.co';
const SUPABASE_ANON_KEY = 'sb_publishable_P5BAs90ZU452EmfBbl0VPw_wz6DIfnN';
let supabase = null;
try {
  if (window.supabase && typeof window.supabase.createClient === 'function') {
    supabase = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
  }
} catch (err) {
  console.error('Supabase initialization failed:', err);
}
const STORAGE_BUCKET = 'product-images';

const DEFAULT_PRODUCTS = [
 {id:'p1',name:'AR Essential T-Shirt',category:'tshirt',price:2500,color:'black',description:'T-shirt Ar store — coupe moderne.',image:''},
 {id:'p2',name:'AR Classic Pant',category:'pant-normal',price:3500,color:'dark',description:'Pant classique confortable.',image:''},
 {id:'p3',name:'AR Baggy Pant',category:'pant-baggy',price:4000,color:'grey',description:'Coupe baggy tendance.',image:''},
 {id:'p4',name:'AR Street Shoes',category:'shoes',price:6500,color:'white',description:'Chaussures streetwear.',image:''},
 {id:'p5',name:'AR Cap',category:'accessories',price:1800,color:'red',description:'Casquette Ar store.',image:''}
];

let products = [];
let cart = [];
let orders = [];
let currentCategory='all';

const $ = id => document.getElementById(id);
$('year').textContent = new Date().getFullYear();

function categoryName(c){
 return {'all':'Toutes les pièces','tshirt':'T-shirt','pant-normal':'Pant — Normal','pant-baggy':'Pant — Baggy','shoes':'Shoes','accessories':'Accessories'}[c]||c;
}
function colorToGradient(color='black'){
 const c=color.toLowerCase();
 const map={black:['#252a31','#050607'],white:['#f2f2f2','#737983'],red:['#7c2029','#18090b'],blue:['#174c8c','#081426'],green:['#235d45','#08150f'],yellow:['#8a731d','#171306'],orange:['#a94e16','#1a0904'],pink:['#a63c69','#18080f'],purple:['#643e9b','#100817'],grey:['#777d87','#17191d'],gray:['#777d87','#17191d'],beige:['#a78c68','#19130b'],dark:['#343942','#0b0d10']};
 const v=Object.keys(map).find(k=>c.includes(k))||'black';
 return `linear-gradient(135deg,${map[v][0]},${map[v][1]})`;
}
function updateBg(){const active=products.filter(p=>p.category===currentCategory)[0]||products[0];$('colorBg').style.background=active?colorToGradient(active.color):'#080a0d';}
function renderFilters(){$('filters').innerHTML=['all','tshirt','pant-normal','pant-baggy','shoes','accessories'].map(c=>`<button class="filter ${c===currentCategory?'active':''}" onclick="showCategory('${c}')">${categoryName(c)}</button>`).join('');}
function renderProducts(){
 const list=currentCategory==='all'?products:products.filter(p=>p.category===currentCategory);
 $('productsTitle').textContent=categoryName(currentCategory);
 $('products').innerHTML=list.length?list.map(p=>`<article class="product"><span class="badge">${categoryName(p.category)}</span><div class="product-img" style="background:${colorToGradient(p.color)}">${p.image?`<img src="${p.image}" alt="${escapeHtml(p.name)}">`:`<div class="product-placeholder">AR</div>`}</div><div class="product-info"><h3>${escapeHtml(p.name)}</h3><p>${escapeHtml(p.description||'Pièce Ar store')}</p><div class="price">${Number(p.price).toLocaleString('fr-FR')} DA</div><button class="add" onclick="addToCart('${p.id}')">Ajouter au panier</button></div></article>`).join(''):`<div class="empty">Aucune pièce disponible dans cette catégorie pour le moment.</div>`;
 updateBg();renderFilters();
}
function escapeHtml(s=''){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
function showCategory(c){currentCategory=c;renderProducts();closeMenu();window.scrollTo({top:document.querySelector('.products').offsetTop-100,behavior:'smooth'});}
function openMenu(){$('sideMenu').classList.add('show');$('overlay').classList.add('show')}
function closeMenu(){$('sideMenu').classList.remove('show');if(!$('cartDrawer').classList.contains('show'))$('overlay').classList.remove('show')}
function toggleSub(id){$(id).classList.toggle('show')}
function closeAll(){closeMenu();closeCart()}
function openCart(){$('cartDrawer').classList.add('show');$('overlay').classList.add('show');renderCart()}
function closeCart(){$('cartDrawer').classList.remove('show');if(!$('sideMenu').classList.contains('show'))$('overlay').classList.remove('show')}
function addToCart(id){const found=cart.find(x=>x.id===id);if(found)found.qty++;else cart.push({id,qty:1});renderCart();$('cartCount').textContent=cart.reduce((a,x)=>a+x.qty,0);openCart();}
function changeQty(id,d){const x=cart.find(i=>i.id===id);if(!x)return;x.qty+=d;if(x.qty<=0)cart=cart.filter(i=>i.id!==id);renderCart();}
function renderCart(){
 $('cartCount').textContent=cart.reduce((a,x)=>a+x.qty,0);let total=0;
 $('cartItems').innerHTML=cart.length?cart.map(x=>{const p=products.find(q=>q.id===x.id);if(!p)return '';total+=p.price*x.qty;return `<div class="cart-row"><div class="cart-thumb">${p.image?`<img src="${p.image}" alt="">`:'AR'}</div><div><b>${escapeHtml(p.name)}</b><div style="color:var(--muted);font-size:11px">${Number(p.price).toLocaleString('fr-FR')} DA</div><div class="qty"><button onclick="changeQty('${p.id}',-1)">−</button><span>${x.qty}</span><button onclick="changeQty('${p.id}',1)">+</button></div></div><button class="remove" onclick="changeQty('${p.id}',-${x.qty})">Supprimer</button></div>`}).join(''):`<div class="empty">Votre panier est vide.</div>`;
 $('cartTotal').textContent=total.toLocaleString('fr-FR')+' DA';
}
async function submitOrder(e){
 e.preventDefault();if(!cart.length){alert('Votre panier est vide.');return;}
 const items=cart.map(x=>{const p=products.find(q=>q.id===x.id);return p?{product_id:p.id,name:p.name,quantity:x.qty,price:Number(p.price)}:null}).filter(Boolean);
 const total=items.reduce((a,x)=>a+x.price*x.quantity,0);const orderId=crypto.randomUUID();
 try{
  const {error}=await supabase.from('orders').insert({id:orderId,customer_name:$('customerName').value,phone:$('customerPhone').value,address:$('customerAddress').value,note:$('customerNote').value,total,status:'Nouvelle'});
  if(error)throw error;
  const {error:itemError}=await supabase.from('order_items').insert(items.map(x=>({order_id:orderId,product_id:x.product_id,quantity:x.quantity,price:x.price})));
  if(itemError){await supabase.from('orders').delete().eq('id',orderId);throw itemError;}
  cart=[];renderCart();alert('Commande enregistrée. Merci !');$('customerName').value='';$('customerPhone').value='';$('customerAddress').value='';$('customerNote').value='';
 }catch(err){console.error(err);alert('Impossible d’enregistrer la commande. Vérifiez Supabase et les policies RLS.');}
}
function openAdmin(){$('adminModal').classList.add('show');renderAdmin();$('adminUser').focus()}
function closeAdmin(){$('adminModal').classList.remove('show')}
async function adminLogin(e){
 e.preventDefault();try{const {error}=await supabase.auth.signInWithPassword({email:$('adminUser').value.trim(),password:$('adminPass').value});if(error)throw error;$('adminPass').value='';renderAdmin();}catch(err){console.error(err);alert('Compte ou mot de passe incorrect.');}
}
async function adminLogout(){await supabase.auth.signOut();renderAdmin()}
async function renderAdmin(){const {data:{user}}=await supabase.auth.getUser();const logged=!!user;$('loginView').classList.toggle('hidden',logged);$('adminView').classList.toggle('hidden',!logged);if(logged){renderAdminProducts();renderAdminOrders();}}
async function loadProducts(){try{const {data,error}=await supabase.from('products').select('id,name,category,price,color,description,image_url,created_at').order('created_at',{ascending:false});if(error)throw error;products=(data||[]).map(p=>({...p,image:p.image_url||''}));}catch(err){console.error(err);products=DEFAULT_PRODUCTS;}renderProducts();renderCart();}
async function addProduct(e){
 e.preventDefault();const file=$('pImage').files[0];try{let image_url='';if(file){const ext=(file.name.split('.').pop()||'jpg').toLowerCase().replace(/[^a-z0-9]/g,'');const path=`${crypto.randomUUID()}.${ext}`;const {error}=await supabase.storage.from(STORAGE_BUCKET).upload(path,file,{upsert:false,contentType:file.type||'image/jpeg'});if(error)throw error;image_url=supabase.storage.from(STORAGE_BUCKET).getPublicUrl(path).data.publicUrl;}const {error}=await supabase.from('products').insert({name:$('pName').value,category:$('pCat').value,price:Number($('pPrice').value),color:$('pColor').value,description:$('pDesc').value,image_url});if(error)throw error;e.target.reset();await loadProducts();renderAdminProducts();alert('Pièce ajoutée.');}catch(err){console.error(err);alert('Impossible d’ajouter la pièce.');}}
async function renderAdminProducts(){const {data,error}=await supabase.from('products').select('id,name,category,price,color,description,image_url,created_at').order('created_at',{ascending:false});if(error){$('adminProducts').innerHTML='<div class="empty">Impossible de charger les produits.</div>';return;}$('adminProducts').innerHTML=(data||[]).length?(data||[]).map(p=>`<div class="admin-product">${p.image_url?`<img src="${p.image_url}" alt="">`:`<div style="width:55px;height:55px;border-radius:10px;background:${colorToGradient(p.color)};display:grid;place-items:center">AR</div>`}<div><b>${escapeHtml(p.name)}</b><div style="font-size:11px;color:var(--muted)">${Number(p.price).toLocaleString('fr-FR')} DA · ${categoryName(p.category)}</div></div><div style="display:flex;gap:5px"><button class="small-btn" onclick="editPrice('${p.id}')">Prix</button><button class="small-btn danger" onclick="deleteProduct('${p.id}')">×</button></div></div>`).join(''):'<div class="empty">Aucune pièce pour le moment.</div>';}
async function editPrice(id){const p=products.find(x=>x.id===id);if(!p)return;const val=prompt('Nouveau prix (DA) :',p.price);if(val===null||isNaN(val)||Number(val)<0)return;try{const {error}=await supabase.from('products').update({price:Number(val)}).eq('id',id);if(error)throw error;await loadProducts();renderAdminProducts();}catch(err){console.error(err);alert('Impossible de modifier le prix.');}}
async function deleteProduct(id){if(!confirm('Supprimer cette pièce ?'))return;try{const {error}=await supabase.from('products').delete().eq('id',id);if(error)throw error;cart=cart.filter(x=>x.id!==id);await loadProducts();renderAdminProducts();}catch(err){console.error(err);alert('Impossible de supprimer cette pièce.');}}
async function renderAdminOrders(){const {data:os,error}=await supabase.from('orders').select('id,customer_name,phone,address,note,total,status,created_at').order('created_at',{ascending:false});if(error){$('adminOrders').innerHTML='<div class="empty">Impossible de charger les commandes.</div>';return;}if(!os?.length){$('adminOrders').innerHTML='<div class="empty">Aucune commande pour le moment.</div>';return;}const ids=os.map(o=>o.id);const {data:is,error:ie}=await supabase.from('order_items').select('order_id,product_id,quantity,price').in('order_id',ids);if(ie){$('adminOrders').innerHTML='<div class="empty">Impossible de charger les détails des commandes.</div>';return;}$('adminOrders').innerHTML=os.map(o=>{const its=(is||[]).filter(i=>i.order_id===o.id);return `<div class="order"><div class="order-head"><b>${escapeHtml(o.id)}</b><span class="status">${escapeHtml(o.status||'Nouvelle')}</span></div><div style="font-size:12px;color:#c4c8d0;margin-top:7px"><b>${escapeHtml(o.customer_name)}</b> · ${escapeHtml(o.phone)}<br>${escapeHtml(o.address)}</div><ul>${its.map(i=>{const p=products.find(x=>x.id===i.product_id);return `<li>${escapeHtml(p?p.name:'Produit')} × ${i.quantity} — ${(i.price*i.quantity).toLocaleString('fr-FR')} DA</li>`}).join('')}</ul><div style="display:flex;justify-content:space-between;align-items:center"><b>${Number(o.total).toLocaleString('fr-FR')} DA</b><div style="display:flex;gap:5px"><button class="small-btn" onclick="setOrderStatus('${o.id}','Confirmée')">Confirmer</button><button class="small-btn" onclick="setOrderStatus('${o.id}','Livrée')">Livrée</button><button class="small-btn danger" onclick="deleteOrder('${o.id}')">Supprimer</button></div></div></div>`}).join('');}
async function setOrderStatus(id,status){const {error}=await supabase.from('orders').update({status}).eq('id',id);if(error){console.error(error);alert('Impossible de modifier le statut.');return;}renderAdminOrders();}
async function deleteOrder(id){if(!confirm('Supprimer cette commande ?'))return;try{let r=await supabase.from('order_items').delete().eq('order_id',id);if(r.error)throw r.error;r=await supabase.from('orders').delete().eq('id',id);if(r.error)throw r.error;renderAdminOrders();}catch(err){console.error(err);alert('Impossible de supprimer cette commande.');}}
// Expose inline-click handlers globally (required by GitHub Pages/browser inline onclick).
window.openCart = openCart;
window.closeCart = closeCart;
window.openMenu = openMenu;
window.closeMenu = closeMenu;
window.closeAll = closeAll;
window.toggleSub = toggleSub;
window.showCategory = showCategory;
window.addToCart = addToCart;
window.changeQty = changeQty;
window.submitOrder = submitOrder;
window.openAdmin = openAdmin;
window.closeAdmin = closeAdmin;
window.adminLogin = adminLogin;
window.adminLogout = adminLogout;
window.addProduct = addProduct;
window.editPrice = editPrice;
window.deleteProduct = deleteProduct;
window.setOrderStatus = setOrderStatus;
window.deleteOrder = deleteOrder;

if (supabase) {
  supabase.auth.onAuthStateChange(()=>renderAdmin());
}
(async()=>{cart=[];renderProducts();renderCart();if(supabase){await loadProducts();await renderAdmin();}else{console.error('Supabase client is unavailable.');}})();
</script>
</body>
</html>
